<h2>New Property</h2>

<%= render "form.html", changeset: @changeset,
                        action: property_path(@conn, :create) %>

<span><%= link "Back", to: property_path(@conn, :index) %></span>
