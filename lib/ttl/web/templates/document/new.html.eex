<h2>New Document</h2>

<%= render "form.html", changeset: @changeset,
                        action: document_path(@conn, :create) %>

<span><%= link "Back", to: document_path(@conn, :index) %></span>
