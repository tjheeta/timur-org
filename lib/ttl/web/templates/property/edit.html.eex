<h2>Edit Property</h2>

<%= render "form.html", changeset: @changeset,
                        action: property_path(@conn, :update, @property) %>

<span><%= link "Back", to: property_path(@conn, :index) %></span>
